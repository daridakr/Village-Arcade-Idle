using UnityEngine;

namespace ForeverVillage.Scripts
{
    public sealed class UpgradesLevelUpper
    {
        private readonly PlayerCoins _playerCoins;

        public UpgradesLevelUpper(PlayerCoins playerCoins)
        {
            _playerCoins = playerCoins;
        }

        //public bool CanUpgrade(PlayerUpgrade playerUpgrade)
        //{
        //    if (playerUpgrade.IsMaxLevel())
        //    {
        //        return false;
        //    }

        //    var price = playerUpgrade
        //}
    }
}